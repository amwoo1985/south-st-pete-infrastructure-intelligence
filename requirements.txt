# South St. Petersburg Infrastructure Intelligence — direct dependencies.
# Pin to what's verified working in .venv (Python 3.12). Transitive deps
# resolve on install; not pinned here to keep this file reviewable.

# Providers (DECISIONS #13)
openai==3.3.1

# Config
python-dotenv==1.2.3

# Crawlers (DECISIONS #14, #19, #20)
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.2
pypdf==6.16.1

# HTTP client for FastAPI's TestClient, Phase H (DECISIONS #24) — not used
# by the crawlers, which stay on `requests` per DECISIONS #14.
httpx==0.28.1

# openai==3.3.1's actual transitive HTTP client (a separate real package
# from `httpx` above, not a typo — see DECISIONS #24's history of a
# broken httpx2/httpcore2 install being mistaken for this). Pinned
# explicitly because tests/embeddings/test_client.py imports it directly
# to build realistic httpx2.Request/Response objects for retry-path tests
# (Phase E, rag-review pre-commit pass).
httpx2==2.12.0

# Postgres + pgvector (Phase E, DECISIONS #71) — psycopg3 with the binary
# wheel (no local libpq build needed); pgvector's psycopg adapter lets a
# `vector` column round-trip as a plain Python list[float].
psycopg[binary]==3.2.10
pgvector==0.5.0

# FastAPI layer, Phase H — pinned to what's verified installed and working
# in .venv this round (`pip install fastapi "uvicorn[standard]"
# python-multipart python-docx`, no upper bounds forced, this is just what
# resolved live).
fastapi==0.141.1
# starlette is fastapi's actual ASGI toolkit dependency (pinned explicitly
# per this repo's "every direct dependency this code imports against is
# pinned" convention — nothing here imports starlette directly today, but
# FastAPI's own exception/response types are re-exports of it).
starlette==1.6.0
# ASGI server used to actually run/verify the app locally (`uvicorn
# app.api.main:app`); [standard] pulls in uvloop/httptools/websockets for
# the realistic production-shaped install, not the bare minimal one.
uvicorn[standard]==0.52.4
# Required by FastAPI's UploadFile/File() multipart form parsing —
# POST /documents/upload.
python-multipart==0.0.32
# DOCX parsing for POST /documents/upload's uploaded-document chunker
# (app/chunking/uploaded_document.py). PDF and TXT parsing reuse the
# already-pinned pypdf and the stdlib — no new dependency needed for
# either of those two formats.
python-docx==1.2.0
